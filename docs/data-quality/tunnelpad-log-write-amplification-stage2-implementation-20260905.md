# TunnelPad 日志低写放大：阶段 2 真实 Release 实施证据

- 日期：2026-09-05
- 阶段：阶段 2
- 结论：真实 Release 回归完成；进入独立完成复核
- 关联计划：[TunnelPad 日志低写放大与流式保留](../plans/tunnelpad-log-write-amplification.md)
- 产物：`/Users/jafish/Documents/work/TunnelPad/dist/TunnelPad.app`

## 真实窗口

- 观测时间：2026-09-05 14:02:15 至 14:32:20（30 分钟，起始样本 + 每分钟样本共 31 个）。
- 环境：最新签名 Release App、现有 `admin-tunnel` 和 `reverse-ssh`；只记录脱敏状态、日志元数据、CPU、Activity Monitor 指标和本地 API 结果。
- 两个隧道在整个窗口中均为 `running`，本地 API 健康结果始终为 `ok`。
- 观察期间不主动制造业务流量，因此真实窗口验证的是正常静默运行的低写入边界；持续追加和跨阈值压缩由阶段 1 fixture 覆盖。

## 日志和 CPU 结果

31 个样本中，以下值保持不变：

| 隧道 | 文件大小 | 修改时间（Unix 秒） | 逻辑行数 | `debug1` 行数 |
|---|---:|---:|---:|---:|
| admin-tunnel | 199,441 字节 | 1,788,587,613 | 2,000 | 1,992 |
| reverse-ssh | 133,715 字节 | 1,788,587,862 | 2,071 | 1,868 |

- App CPU 采样为 `0.0`，仅出现一次 `0.1` 的瞬时采样；两个 SSH 进程的每分钟采样均为 `0.0`。
- 静默窗口没有出现约 10 秒一次的 mtime 更新、文件增长或 `debug1` 增长。
- reverse 日志的 2,071 行是历史文件状态；本窗口未继续增长，阶段 1 的 fixture 已验证跨高水位后压缩回最近 2,000 个逻辑行。

## Activity Monitor 结果

Activity Monitor 在窗口开始、中段和结束均查看了 TunnelPad：

| 指标 | 窗口开始 | 中段/结束 | 结论 |
|---|---:|---:|---|
| 磁盘累计写入 | 约 33 KB | 约 33 KB | 窗口内未增加，未回到旧的持续写放大形态 |
| 磁盘累计读取 | 约 1.0 MB | 约 1.0 MB | 无异常读放大 |
| CPU | — | 0.0 | 与命令行采样一致 |
| 对能耗的影响 | 0.2（开始） | 0.0–0.2（中段/结束） | 处于低影响区间；“12 小时电源”是滚动累计值，不作为本窗口增量结论 |

旧基线为 Activity Monitor 累计写入折算约 57 KiB/s，同时日志大小不变但修改时间周期变化；本次 30 分钟窗口的 TunnelPad 写入显示值保持约 33 KB，未观察到该模式。

## 退出清理与恢复

1. 结束 App 后，9998 端口释放、两个受管 launchd label 均未加载、没有残留 SSH 进程。
2. 重新启动同一 Release App 后，两个隧道均成功启动并保持 `running`，本地 API 为 `ok`。
3. 两个隧道的独立 `-v` 配置摘要仍为 `false`；详细日志开关未被意外打开。

## 与完成条件的对应

- 30 分钟真实窗口通过：日志大小/mtime/行数/`debug1` 稳定，CPU、Activity Monitor、API、隧道状态无回归。
- 追加期间不整文件压缩、高水位批量压缩、锁失败重试、行语义和最近 2,000 行由阶段 1 专项测试与 Swift/Rust 回归覆盖。
- 真实退出清理和用户当前运行状态恢复通过。

本证据不把无业务流量窗口解释为高流量场景性能上限；高流量时的触发边界以阶段 1 的 512 KiB fixture 和实现契约为准。
