# TunnelPad 稳定性阶段 2 启动/退出资源收敛切片独立准入复核

- 日期：2026-09-02
- 阶段：阶段 2
- 切片：`launchd` 启动/退出资源收敛
- 复核类型：实施前独立准入复核
- Step 0 证据：[启动/退出资源收敛切片 Step 0](tunnelpad-stability-stage2-lifecycle-reconciliation-step0-20260902.md)
- 结论：达到“待实施”标准（仅限本切片）

## 复核范围

本轮只准入配置内 `launchd` 资源的启动状态发现、逐条退出清理和 shutdown 单入口门禁，不把 app pidfile、配置外进程扫描或跨层 UI/Rust 迟到任务治理误计入本切片。

## 准入门槛

| 门槛 | 结论 | 证据 |
|---|---|---|
| 当前目标、范围和非目标明确 | 通过 | Step 0 固定启动只读快照、退出逐条收敛、app/配置外资源非目标。 |
| 本阶段 Step 0 完整 | 通过 | Step 0 记录当前 `TunnelManager`/Rust owner/Shutdown 调用链和已发现的首错即停缺陷。 |
| 样本/fixture 矩阵可执行 | 通过 | 8 行矩阵覆盖启动无 UI、快照失败、全部清理、单条失败继续、在途取消、并发 shutdown、信号/正常退出和非目标隔离。 |
| 实现顺序和公共契约明确 | 通过 | 只调整 owner 内部 shutdown 入口和已有 JSON 结果；不新增 C ABI 版本、配置字段、plist 命令或第二 owner。 |
| 验证方式明确 | 通过 | Rust owner 单测、Swift 启动 fixture、全量 Swift/Rust、治理和 GitNexus 变更范围检查。 |
| 失败/回滚边界明确 | 通过 | 单条失败后继续清理，未确认 `notLoaded` 不计成功；owner 关闭后拒绝新操作；隔离回滚。 |
| 影响范围已核对 | 通过 | `CoreOwner.shutdown` upstream impact 为 CRITICAL，已记录为生命周期枢纽；修改限定在本切片并要求补齐所有受影响回归。 |
| 共享编辑边界明确 | 通过 | 不修改日志事件流、备注、ECS 前置顺序和跨层 UI 状态模型；`TunnelManager` 只补启动状态 fixture所需最小路径。 |
| 当前切片无未解决阻塞项 | 通过 | 现状和安全边界已冻结；后续跨层一致性与 app 计划不阻塞本切片。 |
| 治理结构完整 | 通过 | 本记录追加到稳定性专项，完成后同步 `PLAN_MAP.md`、实施证据和验证结果。 |

## 独立结论

本轮确认启动/退出资源收敛切片的目标、范围/非目标、Step 0、样本矩阵、实现顺序、验证方式、失败/回滚边界和影响分析均已明确，达到“待实施”标准。

该结论不代表阶段 2 整体完成，也不替代 ECS 自动恢复切片或后续跨层状态一致性切片的证据。实现完成后仍需基于当前代码、专项 fixture、全量回归、反向引用和 GitNexus `detect_changes()` 独立核对，不得仅依据本文件或计划状态判定完成。
