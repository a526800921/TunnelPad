# 阶段 3 独立完成复核：无人值守受管 SSH 收敛恢复

日期：2026-09-04

## 复核范围

本复核基于当前仓库代码、回归输出、Release 产物校验、治理检查和真实受控验收证据进行只读核对。复核对象是阶段 3 的真实无人值守恢复闭环，不把历史失败尝试或实施者自述单独作为完成依据。

## 逐项核对

| 完成条件 | 独立核对结果 |
|---|---|
| 受管身份核验和 PID 复用保护 | 通过；路径读取修复后增加当前 PID 回归，身份未知/变化继续禁止发信号 |
| 有界状态与停止收敛 | 通过；状态查询与 bootout 均有 2 秒边界，信号升级逐步重新核验身份 |
| 无人工真实恢复 | 通过；单次目标 `SIGSTOP` 后未人工释放、启停或重启 App，launchd 自动完成旧 PID 收敛和新 PID 拉起 |
| 启动、探针和非目标隔离 | 通过；目标恢复到 `running` 与 HTTP `401/satisfied`，`reverse-ssh` PID 保持不变，App 保持存活 |
| ECS 与外部资源边界 | 通过；仅做只读 preflight，未扩大远端规则或写入配置 |
| 自动冷却和既有契约 | 通过；沿用阶段 1/2 已复核的 generation、取消、fail-closed、冷却和多隧道隔离边界 |
| 回归与发布门禁 | 通过；Rust 76+1、Swift 139/139、Release 构建/签名校验、治理检查和变更范围检查均纳入最终验证 |

## 结论

**通过，阶段 3 完成；本专项计划达到“已完成”条件。**

本结论只覆盖已声明的受管 `admin-tunnel` 无人值守恢复范围，不扩展为未知进程处置、远端配置写入、公共 API/ABI 变更或跨 App 持久化冷却。

复核者：Codex（独立只读完成复核）
