# 阶段 2：用户实际重启后的只读验收

日期：2026-09-12。代码基线ca3463d。用户选择“我直接重启吧”，已被告知当前进程只读云护栏不跨重启；随后报告“重启了”。原拟临时修改默认外部配置方案未执行。本轮仅只读观测，不发送启动/重启命令、不改配置或云端资源。

## 实际证据

- kern.boottime：2026-09-12 21:26:28（Asia/Shanghai）。
- 新版App PID7169，PPID1，21:27:14启动，路径dist/stage1-validation/TunnelPad.app/Contents/MacOS/tunnelpad。
- App日志：21:27:15建立启动恢复队列，21:27:17“已确认运行；交接健康监测”。
- 进程环境无TUNNELPAD_CONFIG_FILE和CFFIXED_USER_HOME覆盖，确认本次使用正常配置，不继承临时只读云护栏。
- 原配置SHA-256仍为198cf1d8510674cb44faa47454a5ca815ad76169a37200c1b009bb8651b7fdf6。唯一autoStart=true仍为motorcycle-local-docker。
- admin-tunnel、reverse-ssh均由launchctl确证未加载。
- 目标launchd确证running，当前SSH PID9120，runs=2、last exit code=255。lsof确认PID9120存在ESTABLISHED TCP及127.0.0.1:10080监听。
- 初次API仍报告前一个PID8020，不能把缓存PID当fresh证据。目标曾重连一次，原因尚未确证；当前以launchctl/lsof实测为准。
- 当前无pending journal。没有审计本次正常前置是否实际执行云写，不能声称重启窗口云写为零。

## 边界

本次证实正常系统启动后的新版App与唯一候选自动运行。没有注入“登录时物理离线”，不能声称该复合场景实测；登录链路与此前真实时间前置故障恢复分别已有证据。HTTP401仅代表配置端点响应，不单独证明SSH业务健康。

当前技术证据已由reboot_review独立只读复核通过；最终用户体验接受及是否全程无手动启动仍待用户确认。探针误导按用户要求延后修复；API缓存PID现象另行记录，不掩盖实际重连。

追加稳定性只读观察：间隔20秒的两次launchctl/lsof均为PID9120、runs=2、TCP已连接且10080监听；未见该观察窗口内继续重启。SSH日志尾部含历史错误，不据此断言本次exit255的具体原因。


## 独立只读复核

复核者reboot_review，风险高影响，结论：通过；本次重启技术观察无必须修复项，阶段仍待用户接受。独立核实boot/App时间、UTC日志两条事件、配置哈希、唯一候选、非目标未加载、目标PID9120连接及监听、测试变量缺失和journal为0。复核时API已追平PID9120；先前8020仅是当时缓存差异，不能称持续不更新。

复核未修改文件、控制进程或调用云API；上述证据限制仍有效。下一动作：等待用户验收。

## 用户验收与最终关闭

2026-09-12，在被明确询问“重启后没有手动打开TunnelPad，也没有点击隧道启动，对吗？”后，用户回答“是的”。该回复确认本次实际重启后无人工启动，补齐独立技术观察所需的用户侧验收。

阶段2和本专项计划据此关闭。完成依据为已分别验证的持续前置故障恢复、取消/退出、三种资源状态观测，以及实际系统重启后自启与用户确认；不追加声称物理断网登录组合场景、全天能耗或SSH业务端到端探针已经实测。探针误导问题按用户明确要求保留后续统一修复，API短暂PID缓存差异保留待评估。此次仅文档收口，没有修改代码、运行实例或云资源。
