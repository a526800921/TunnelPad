# TunnelPad 能耗修复后隔夜对比基线

- 日期：2026-09-05（Asia/Shanghai），采集窗口 19:14–19:18。
- 关联计划：[后台健康监测能耗优化](../plans/tunnelpad-health-monitor-energy.md)、[日志保留能耗回归](../plans/tunnelpad-log-retention-energy-regression.md)（两计划均已关闭）。
- 性质：只读观测基线，为 2026-09-06 隔夜对比提供当日锚点；不是准入复核，不改变任何计划状态。
- 操作边界：未重启 App、未启停隧道、未清空或修改真实日志、未修改源码。
- 机器：Mac17,9，macOS 26.6.2，物理内存 64 GB，电池 80%（接交流电，未充电，交流电时长 46:03→46:06），无热告警。

## 运行产物核验

- App 进程 PID `55844`，启动于 `2026-09-05 14:33:24`，采集时已运行约 4 小时 41 分。
- 仓库 HEAD 为 `e0d3defddbbcd4bfc16b5a292294c8279a93b2e7`，工作树干净。
- `dist/TunnelPad.app` 二进制修改时间 `13:53:03`，与 `.build/arm64-apple-macosx/release/tunnelpad` 的 Mach-O UUID 一致：`3621126E-DE12-3F6E-87E6-C0AFC42A8788`。
- `Sources/TunnelPadCore/LogEventStore.swift` 修改时间 `13:51:14` 早于构建时间，确认运行中的构建包含日志保留（CRLF/增量读取）修复。
- 隧道：launchd 注册的 `admin-tunnel`（PID `56123`）与 `reverse-ssh`（PID `56201`）均 `running`，与 App 同批启动（约 4 小时 47 分）；本机 API（端口 9998）显示 `admin-tunnel` 探针 `enabled`、`401/satisfied`、`throttleInterval` 10 秒，`reverse-ssh` 探针 `disabled`。
- 两个 SSH 进程各 `%CPU 0.0`、RSS 约 4,750–4,800 KB；launchd SSH 参数不含 verbose（以 reverse-ssh 进程参数为证），不再产生逐连接 channel 调试输出。

## CPU

采样命令（与 09-05 上午复验同方法）：

```bash
top -l 66 -pid 55844 -stats pid,cpu,threads,time -s 1
```

- 窗口 `19:15:45–19:16:59`（约 74 秒），66/66 个样本。
- 分布：`0.0%` ×59、`0.5%` ×2、`0.6%` ×1、`0.7%` ×4；最大 `0.7%`；线程 6–7；累计 CPU 时间 `00:14.39`（折合进程生命周期平均约 0.5%）。
- 对照：上午复验记录为约 10 秒周期、17.6%–26.6% 的短峰；本窗口无周期峰。
- 系统整体：Load Avg `2.44 / 2.85 / 2.91`；`CPU usage 6.43% user, 6.20% sys, 87.35% idle`（top 连续第二样本）。

## 内存

- 进程：RSS `150,224 KB`（约 146.7 MiB），`%MEM 0.2`，VSZ `436,038,528 KB`（虚拟地址空间，不占物理内存），状态 `S`。
- 系统：`PhysMem 62G used（wired 3,852M，compressor 2,529M），unused 1,287M`；`vm.swapusage used = 0.00M`；页大小 16,384 字节；`vm_stat`：free 39,359 页、active 1,844,032 页、inactive 1,835,113 页。

## 磁盘与日志

- 数据卷：`/System/Volumes/Data` 926Gi 总量、333Gi 已用、571Gi 可用（37%）。
- `iostat -d -c 5 -w 2`（系统级，未做进程归因）：disk0 各 2 秒样本 `MB/s` 1.14 / 1.07 / 0.03 / 0.68 / 0.10，tps 40 / 85 / 1 / 24 / 6。
- 日志元数据（只统计大小与 mtime，不读取正文）：

| 文件 | 字节数 | mtime |
|---|---:|---|
| admin-tunnel.log | 199,441 | 13:53:33 |
| reverse-ssh.log | 133,715 | 13:57:42 |
| demo-sleep.log | 0 | （昨日残留空文件） |

- 74 秒采样窗口内与 19:18 复查均零增长；App 14:33 启动以来两个隧道日志 mtime 未变化，即零写入。探针每 10 秒仍在运行（API 证据见上），与上午根因记录一致：触发输入（verbose channel 日志）已随 verbose 关闭消失。

## 能耗（活动监视器能耗页）

视图「过去12小时内使用过的应用程序」，列：对能耗的影响 / 12小时电源 / App小憩 / 防止睡眠。

| App | 对能耗的影响（两次读数） | 12小时电源 |
|---|---|---:|
| TunnelPad | 0.5 → 0.0 | 184.97（两次一致） |
| ZCode | 2.5 → 2.7 | 18.14 |
| Code | 1.8 → 0.1 | 2.86 |
| Google Chrome | 0.4 → 1.9 | 45.66 |
| ModelPad | 0.2 → 0.0 | 0.21 |

- TunnelPad `App小憩 = 否`、`防止睡眠 = 否`；瞬时能耗影响 0.0–0.5，低于日志修复后完成复核时的约 2.0。
- 解读：12 小时电源为滚动平均，当前 184.97 高于上午 11:37 的 131.33 与完成复核时的约 129.26。该窗口仍包含当日 07:17–13:53 未修复运行期，且原缺陷成本随日志增长放大，数值升高与机制一致；此列不能作为修复效果指标，明日随旧数据滑出窗口的回落才是对比信号。
- `powermetrics` 需要 sudo 密码，本轮未采集瓦数级数据。

## 2026-09-06 复测清单

1. `ps -o pid,%cpu,%mem,rss,etime -p <PID>`（若 App 重启过则按新 PID）。
2. `top -l 66 -pid <PID> -stats pid,cpu,threads,time -s 1`——预期仍无 >2% 周期峰。
3. `stat -f '%N %z mtime %Sm' ~/Library/Logs/TunnelPad/*.log`——记录 24 小时增量，预期在 2000 行保留上限内有界。
4. 活动监视器能耗页 TunnelPad 读数 ×2——预期 12 小时电源显著低于 184.97，能耗影响保持个位数量级。
5. `curl http://127.0.0.1:9998/api/tunnels`——预期两隧道 `running`，`admin-tunnel` 探针 `401/satisfied`。
6. 若任一预期落空，按完成复核记录的边界以新的真实运行栈和日志元数据另立回归记录，不覆盖历史证据。

## 文档检查

- `git diff --check`：通过。
- `plan-governance-cli check .`：通过。
- 本记录为观测基线，未改变任何计划状态；PLAN_MAP「完成证据」表已加入对应链接。

## 2026-09-06 复测结果（追加）

- 时间：11:13–11:16。App 未重启，仍为 PID `55844`（连续运行 20 小时 40 分）；两个 launchd SSH 进程 PID `56123`/`56201` 亦未重建。仓库 HEAD 未变（`e0d3def`），无源码变更。

| 复测预期 | 结果 | 证据 |
|---|---|---|
| CPU 无 >2% 周期峰 | 通过 | `top -l 66 -pid 55844 -stats pid,cpu,threads,time -s 1`（11:14:07–11:15:21）：`0.0%` ×59、`1.2%`–`1.5%` ×7；非零样本位于第 5/15/24/33/42/51/60 位，间隔恰 9 样本 ≈ 10 秒，对应探针 `throttleInterval` 10 的正常工作；max `1.5%`、窗口均值 `0.15%`；累计 CPU 时间 `01:34.85`（生命周期平均约 0.13%） |
| 日志增量有界 | 通过（零写入） | 三日志大小与 mtime 与 09-05 记录完全一致（admin-tunnel 199,441 B / 13:53:33；reverse-ssh 133,715 B / 13:57:42）；74 秒采样窗口与隔夜全程零写入 |
| 12 小时电源显著回落、能耗影响个位数 | 通过 | 活动监视器能耗页（视图同基线）两次读数：12 小时电源 `184.97 → 0.39`（两次一致）；能耗影响 `0.0 → 0.6`；`App小憩 = 否`、`防止睡眠 = 否` |
| 双隧道 running + admin 探针 401/satisfied | 通过 | 本机 API（9998）：`admin-tunnel`/`reverse-ssh` 均 `running`，探针 `enabled`/`401`/`satisfied`/`throttleInterval 10` 与 `disabled`，与基线一致 |
| 内存与磁盘无异常 | 通过（记录在案） | RSS `166,992 KB`（约 163.1 MiB，较基线 +16.4 MiB）；系统 `PhysMem 55G used`、unused `8,497M`、compressor `1,437M`（较基线下降）、swap `0`；数据卷 329Gi 已用；`iostat` disk0 背景水平与基线同量级（0.03–1.08 MB/s） |

- 结论：修复后第一个完整 12 小时窗口的全部对比预期满足。12 小时电源 `0.39` 位于同屏 App 末端区间（介于终端 `0.26` 与小米互联服务 `0.33` 之间），较基线 `184.97` 下降约 99.8%，验证了基线解读——昨晚读数偏高是滚动窗口仍含未修复期所致。10 秒节奏的单样本 ≤1.5% 毛刺占空比约 `0.13%`，对照修复前同节奏 `17.6%`–`26.6%` 持续峰。无新增回归，按复测清单第 6 条不另立回归记录。
