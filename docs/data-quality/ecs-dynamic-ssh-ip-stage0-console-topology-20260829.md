# ECS 动态 SSH 公网 IP 同步：阶段 0 控制台拓扑基线

日期：2026-08-29
性质：用户提供的 ECS 控制台截图的脱敏观察记录；不是 Workbench 实连、SSH 实连或安全组写入证据。

## 范围与安全边界

- 仅观察用户提供的“网络与安全”页面截图；未操作控制台、未导出配置、未调用 API 或写接口。
- 不记录实例、网卡、安全组、账户或公网地址的完整标识与原始值。
- 截图中的现有规则均视为既有规则；未执行新增、修改、删除或优先级调整。

## 观察结果

| 观察项 | 结果 | 结论 |
|---|---|---|
| 网卡拓扑 | 展示一个主网卡，辅助网卡数量为 0 | 当前控制台基线未显示额外网卡路径。 |
| 安全组关联 | 页面展示的安全组与阶段 0 已通过 API 读取的目标一致 | 结合 API 规则快照，可作为当前关联基线；后续若新增网卡或安全组，必须重新反查。 |
| SSH 入方向 | 页面展示两条 SSH `/32` 规则，且没有 `0.0.0.0/0` 的 SSH 规则 | 与 API 汇总一致；两条既有规则都未被本计划证明为受管动态规则，必须保留。 |
| 非 SSH 规则 | 页面展示的 HTTP、HTTPS 与 ICMP 规则不属于本计划的管理范围 | 不作为动态 SSH 同步的删除、修改或合规结论对象。 |
| 恢复入口 | 页面有“远程连接”入口，用户随后确认 Workbench 可用 | 可作为受控实跑的恢复通道；不替代实跑后的 SSH 验证。 |

## 下一步

1. 用户已选择以新专用 RAM 身份在本机直接调用 ECS API，不增加中央执行器；其 `AuthorizeSecurityGroup` IAM 范围不能缩小到目标安全组和 SSH 端口，作为已确认边界记录。
2. 补齐并验证本机直调所需动作后，才进行项目外受控实跑。
3. 后续写权限与受控实跑必须以新增的精确动态规则为对象，不得删除这里观察到的任何既有 SSH 规则。
