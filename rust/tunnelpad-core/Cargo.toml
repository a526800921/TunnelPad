[package]
name = "tunnelpad-core"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
name = "tunnelpad_core"
crate-type = ["staticlib", "rlib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
libc = "0.2"
